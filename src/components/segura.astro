---
// src/components/MainLayout.astro
import astroLogo from '../assets/iconosegura.jpg';
const {
  empresa = "EMPRESA",
  icono = astroLogo.src,
  // icono = "/iconosegura.jpg",
  onConfiguracion = "configuracion()",
  onNuevaEmpresa = "nueva_empresa()",
  onNuevoUsuario = "nuevo_usuario()",
  onFichar = "fichar()",
  onFicharMultiple = "fichar_multiple()",
  onAbrirListado = "abrir_listado()",
} = Astro.props;
---

<div class="flex flex-col items-center justify-center min-h-screen bg-white pt-10">
  <!-- Título -->
  <h1 class="text-[24px] font-serif text-center mb-6">
    {empresa}
  </h1>

  <!-- Imagen -->
<img id="logo" src={astroLogo.src} />
</a
			>


  <!-- Botones -->
  <div id="botones">
    <button
      
     
      onclick={onNuevaEmpresa}
    >
      Alta de nueva empresa
    </button>

    <button
      
      onclick={onNuevoUsuario} 
    >
      Alta de nuevo operario
    </button>

    <button
      class="w-[250px] h-[50px] bg-blue-600 text-white rounded shadow items-center mb-4"
      onclick={onFichar}
    >
      Fichaje individual
    </button>

    <button
      class="w-[250px] h-[50px] bg-blue-600 text-white rounded shadow items-center mb-4"
      onclick={onFicharMultiple}
    >
      Fichaje diario
    </button>

    <button
      class="w-[250px] h-[50px] bg-blue-600 text-white rounded shadow mb-4"
      onclick={onAbrirListado}
    >
      Abrir listados
    </button>
  </div>
</div>
<style>
#logo {
    border-radius: 50%;
    width: 150px;
    height: 150px;
    align-items: center;
    margin-bottom: 20px;

  }
  #botones {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 16px;
    margin-top: 5vh;
  }
</style>